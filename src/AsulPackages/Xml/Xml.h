#ifndef XML_PKG_H
#define XML_PKG_H

namespace asul {

class Interpreter;

// Register the xml package with the interpreter
void registerXmlPackage(Interpreter& interp);

} // namespace asul

#endif // XML_PKG_H
