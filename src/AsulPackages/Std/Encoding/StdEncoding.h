#ifndef STD_ENCODING_H
#define STD_ENCODING_H

namespace asul {

class Interpreter;

// Register the std.encoding package with the interpreter
void registerStdEncodingPackage(Interpreter& interp);

} // namespace asul

#endif // STD_ENCODING_H
