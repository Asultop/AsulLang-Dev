#ifndef STD_ARRAY_H
#define STD_ARRAY_H

namespace asul {

class Interpreter;

// Register the std.array package with the interpreter
void registerStdArrayPackage(Interpreter& interp);

} // namespace asul

#endif // STD_ARRAY_H
