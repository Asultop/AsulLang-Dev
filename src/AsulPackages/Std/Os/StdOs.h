#ifndef STD_OS_H
#define STD_OS_H

namespace asul {

class Interpreter;

// Register the std.os package with the interpreter
void registerStdOsPackage(Interpreter& interp);

} // namespace asul

#endif // STD_OS_H
