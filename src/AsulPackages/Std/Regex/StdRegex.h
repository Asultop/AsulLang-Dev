#ifndef STD_REGEX_H
#define STD_REGEX_H

namespace asul {

class Interpreter;

// Register the std.regex package with the interpreter
void registerStdRegexPackage(Interpreter& interp);

} // namespace asul

#endif // STD_REGEX_H
