#ifndef OS_PKG_H
#define OS_PKG_H

namespace asul {

class Interpreter;

// Register the os package with the interpreter
void registerOsPackage(Interpreter& interp);

} // namespace asul

#endif // OS_PKG_H
