// super 关键字教程：演示子类调用父类方法
// 包含：super.method 语法、多继承中的 super、constructor 链式调用

println("== super 关键字教程 ==");

// -------------------------------------------------------
// 1) 基本 super.method：子类调用父类方法
// -------------------------------------------------------
println("\n--- 1) 基本 super.method ---");

class Animal {
    function constructor(name) {
        this.name = name;
    }

    function speak() {
        println("  ${this.name} 发出声音");
    }

    function describe() {
        return "动物: " + this.name;
    }
}

class Dog <- Animal {
    function constructor(name, breed) {
        // 调用父类 constructor
        super.constructor(name);
        this.breed = breed;
    }

    function speak() {
        // 调用父类的 speak 方法
        super.speak();
        println("  ${this.name} (${this.breed}) 汪汪叫");
    }

    function describe() {
        let base = super.describe();
        return base + ", 品种: " + this.breed;
    }
}

let dog = new Dog("旺财", "金毛");
dog.speak();
println("  ${dog.describe()}");

// -------------------------------------------------------
// 2) super 调用父类 constructor 与方法
// -------------------------------------------------------
println("\n--- 2) super 调用父类 constructor ---");

class Shape {
    function constructor(color) {
        this.color = color;
    }

    function info() {
        return "颜色: " + this.color;
    }
}

class Circle <- Shape {
    function constructor(color, radius) {
        super.constructor(color);
        this.radius = radius;
    }

    function info() {
        let base = super.info();
        return base + ", 半径: " + this.radius;
    }

    function area() {
        let pi = 3.14159;
        return pi * this.radius * this.radius;
    }
}

let c = new Circle("红色", 5);
println("  ${c.info()}");
println("  面积: ${c.area()}");

// -------------------------------------------------------
// 3) 多继承中的 super：按声明顺序查找
// -------------------------------------------------------
println("\n--- 3) 多继承中的 super ---");

class Logger {
    function log(msg) {
        println("  [LOG] ${msg}");
    }
}

class Serializable {
    function serialize() {
        return "{ serialized: true }";
    }
}

class Model <- (Logger, Serializable) {
    function constructor(data) {
        this.data = data;
    }

    function save() {
        super.log("保存数据: " + this.data);
        let json = super.serialize();
        println("  序列化结果: ${json}");
    }
}

let model = new Model("用户数据");
model.save();

// -------------------------------------------------------
// 4) extends 扩展中使用 super
// -------------------------------------------------------
println("\n--- 4) extends 扩展中的 super ---");

class Base {
    function greet() {
        return "你好";
    }
}

class Child <- Base {
    function greet() {
        let base = super.greet();
        return base + "，世界！";
    }
}

// 扩展 Child，添加新方法并调用原有方法
extends Child {
    function formalGreet() {
        let informal = super.greet();
        return "[正式] " + informal;
    }
}

let child = new Child();
println("  ${child.greet()}");
println("  ${child.formalGreet()}");

// -------------------------------------------------------
// 5) super 与 extends 扩展配合
// -------------------------------------------------------
println("\n--- 5) super 与 extends ---");

class Vehicle {
    function constructor(make, year) {
        this.make = make;
        this.year = year;
    }

    function details() {
        return this.make + " " + this.year;
    }
}

class Car <- Vehicle {
    function constructor(make, year, doors) {
        super.constructor(make, year);
        this.doors = doors;
    }

    function details() {
        let base = super.details();
        return base + ", " + this.doors + "门";
    }
}

let car = new Car("丰田", 2024, 4);
println("  ${car.details()}");

// extends 中使用 super
extends Car {
    function summary() {
        let d = super.details();
        return "[车辆] " + d;
    }
}

println("  ${car.summary()}");

println("\n=== super 关键字教程完成 ===");
