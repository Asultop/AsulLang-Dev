// Deque/Stack/Set examples
let d = deque();
d.push(1,2,3);
println("deque size after push:", d.size());
println("pop:", d.pop());
println("shift:", d.shift());
println("peek front:", d.peek());
println("size:", d.size());

let s = stack();
s.push(10);
s.push(20);
println("stack peek:", s.peek());
println("stack pop:", s.pop());

let st = set();
st.add("x");
st.add("y");
println("set has x:", st.has("x"));
println("set values:", st.values());
